[package]
publish = false
name = "garmr-policy"
description = "Explicit access-policy engine (Phase 5): evaluate an access against versioned allow/deny/require policies, SEPARATE from anomaly detection, with an explainable decision where explicit deny overrides learned-normal behavior. Includes policy simulation over historical accesses. Pure, no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
garmr-core.workspace = true
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
blake3.workspace = true
